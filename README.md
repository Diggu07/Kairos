# Kairos
Kairos is a Java-based desktop productivity application that integrates notes, tasks, and reminders into a single platform. It uses Apache OpenNLP to process natural language input and automatically convert user text into structured tasks and reminders.
